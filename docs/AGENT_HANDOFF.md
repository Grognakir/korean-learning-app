# Передача контекста и журнал работ

Этот файл — общий источник контекста для всех моделей, работающих с проектом. Его нужно читать в начале задачи и обновлять после значимых изменений, особенно после публикации.

Не добавлять сюда пароли, ключи, токены и другие секреты.

## Как обновлять файл

1. Новые устойчивые особенности проекта добавлять в раздел «Постоянный контекст».
2. Незавершённые задачи поддерживать в разделе «Открытые задачи».
3. Выполненную работу записывать в начало журнала: дата, результат, коммит, проверки и состояние production.
4. Не писать «выложено на прод», пока не проверены все применимые этапы:
   - коммит отправлен в `origin/main`;
   - deployment рабочего Vercel-проекта завершился успешно;
   - изменения контента синхронизированы с production Supabase;
   - рабочий домен открыт и показывает новые данные.

## Постоянный контекст

### Production

- Рабочий домен: `https://korean-learning-gray.vercel.app`.
- Рабочий Vercel-проект: `korean-learning`.
- GitHub также создаёт deployment `korean-learning-app`; проверять нужно прежде всего рабочий проект и домен выше.
- Production Supabase-проект называется `korean-learning-app`, ref: `zlpxtmjeajntvbydthun`.
- Локальный `.env.local` указывает на `http://127.0.0.1:54331`. Запуск `pnpm import:textbook` с этой конфигурацией не обновляет production.
- Деплой Vercel не импортирует содержимое файлов `content/reference/**` в Supabase автоматически. Изменения учебных JSON нужно отдельно и точечно переносить в `public.textbook_pages` production-базы.
- Перед изменением production-базы сначала выбирать строку по учебнику, уроку и номеру страницы, проверять `source_photo` и идентификаторы блоков, затем обновлять найденную строку и читать её обратно.

### Учебный контент

- Источником истины служат фотографии в `content/reference/inha_book_photo/`. Нельзя дополнять текст, словари или упражнения «по смыслу», если этого нет на фотографии или пользователь явно не попросил.
- Расшифрованный контент лежит в `content/reference/inha_book_content/`.
- `page_index` в `textbook_pages` сквозной в пределах всего учебника. Нельзя вычислять production-строку только по индексу; безопаснее использовать textbook slug, `lesson_number` и `page_number`.
- Уроки читаются приложением из Supabase, поэтому локальное изменение JSON само по себе не появляется даже в локальном браузере, если он подключён к старым данным базы.
- Внутри одного раздела переводной чип словарного слова должен появляться только при первом употреблении.
- Рукописные словари нужно сохранять отдельно от локальных переводов слов, необходимых только конкретному упражнению.

### Проверка публикации

- Статус deployment можно проверить по статусам GitHub-коммита: успешным должен быть как минимум `Vercel – korean-learning`.
- После обновления учебного контента открыть точный раздел на рабочем домене и проверить видимый текст, варианты и правильные ответы.
- Для интерактивных заданий недостаточно увидеть варианты: нужно выбрать правильный ответ и получить «Верно».

## Открытые задачи

- Для кнопки «Изучить подробнее» в грамматическом блоке ещё нет целевой страницы; `details_link.href` пока равен `null`, кнопка отключена.

## Журнал выполненных работ

### 2026-09-23 — Добавлен `2급 4과` («날씨와 생활»)

- По 15 фотографиям (`IMG_0647`–`IMG_0661`) дословно расшифрован весь урок 4: 준비하기1 (V-네요, V-(으)ㄴ/는/(으)ㄹ 것 같다), 준비하기2 (DV-아/어/여지다, AV-기 시작하다, N(으)로③), 말하기, 듣고 말하기, 읽고 말하기, 쓰기, 문화 이해하기 (+발음).
- Новый файл `content/reference/inha_book_content/2급_lesson_04/lesson-04.json`, зарегистрирован в `scripts/import-textbook.ts` (аудио 213–216 подключены; отдельных иллюстраций для этого урока нет — везде `illustration: null`, `storage_path: null`, задел на будущее).
- Найден и исправлен баг: таблицы понимания текста (읽고 말하기, вопросы 1–2) изначально были заполнены готовыми ответами в `table.rows` — компонент `TableQuestion` на самом деле трактует `rows` как подписи строк с пустыми полями ввода для студента, а не как содержимое. Поправлено на `rows: ["봄","여름","가을","겨울"]`, `columns: [""]`.
- `tsc`/`lint`/216 тестов зелёные. Локальный импорт (`npm run import:textbook`) выполнен и проверен на рабочем домене DEV (localhost) — 준비하기1, 읽고 말하기 (таблицы), 문화 이해하기 (+발음) визуально подтверждены.
- Выложено на прод отдельным продолжением в тот же день: коммит `fdb3dc7` (`Add 2급 lesson 4 (날씨와 생활) content`) отправлен в `origin/main`; deployment `Vercel – korean-learning` завершился успешно (Ready, Production). `textbook_pages` для `inha-2`/урок 4 (`page_index` 43–57) и аудио 213–216 перенесены в production через `npm run import:textbook` с production-переменными (полный upsert всего учебника, не точечный — идемпотентен, старые уроки не задеты). Содержимое проверено напрямую через REST (диалог 준비하기1 — 9 строк, таблицы 읽고 말하기 — подписи строк 봄/여름/가을/겨울 с пустой колонкой, 발음 — примеры на месте). Рабочий домен `korean-learning-gray.vercel.app` не проверен вручную через интерфейс — `/learning/**` требует логина, учётных данных пользователя нет; проверка ограничена REST + статусом deployment.

### 2026-09-23 — Исправлено чтение в `2급 3과 → 읽고 말하기`

- По фотографии `IMG_0625` заново дословно перенесены текст страницы 52, пропуск `(ㄱ)`, оба вопроса, варианты ответов и заключительный вопрос для обсуждения.
- Удалён ранее придуманный текст про начало обучения в 18 лет, тренировки с рассвета и отсутствие побед: этих сведений на фотографии нет.
- Правильные ответы: `되기로 했습니다` и `이 사람은 자동차 경주에서 우승하고 싶어 합니다.`.
- Добавлены регрессионные тесты для текста и заданий страницы 52.
- Коммит: `be0d0ae` (`Correct lesson 3 reading from source photo`).
- `origin/main` обновлён; deployments `Vercel – korean-learning` и `Vercel – korean-learning-app` завершились успешно.
- Страница 52 точечно обновлена в production Supabase.
- На рабочем домене подтверждены новый текст и два сообщения «Верно» для правильных вариантов.

### 2026-09-23 — Правки `2급 1과 → 준비하기 1`

- В рукописном словаре оставлены только `개월`, `앞으로`, `지내다`, `자세하다`.
- Уточнены `N(이)라고 하다` и `AV-(으)ㄴ 지`, исправлено выделение грамматических частей.
- Добавлен опциональный элемент «Изучить подробнее» без активной ссылки до появления целевой страницы.
- Добавлены локальные переводы слов для примеров упражнений без загрязнения рукописного словаря.
- Реализована дедупликация переводных чипов в пределах раздела.
- Коммит: `a958a52` (`Highlight grammar-in-use in dialogues and dedupe vocab chips page-wide`).

### 2026-09-22 — Тема «Вопросительные слова» и тренажёры

- Расширено содержание `/topics/questions`, улучшена боковая навигация и визуальные отступы.
- Во всех тренажёрах правильный ответ сразу переводит дальше; после ошибки объяснение сохраняется.
- Три действия на итоговом экране оформлены как полноценные кнопки.
- Добавлены недостающие переводные подсказки в `2급 3과 → 준비하기 2`.
- Выявлена важная особенность: первоначальный импорт через `.env.local` обновил локальную, а не production-базу. После этого нужная строка была обновлена точечно в production Supabase и проверена на рабочем домене.
- Основные коммиты: `3f03ecf`, `52ceb46`, `bb1151a`.

### 2026-09-21 — Учебник 2급 и конвейер контента

- Полностью добавлены уроки 1–3 второго уровня: разделы подготовки, говорения, аудирования, чтения, письма, культуры и произношения.
- Добавлены и подключены типы блоков `comprehension_exercise`, `writing_exercise` и `pronunciation`.
- Исправлена работа `page_index`: он является сквозным для всего учебника, а не начинается заново в каждом уроке.
- При повторном полном импорте учебника старые строки могут оставаться на прежних индексах, потому что `upsert` их не удаляет. Перед таким импортом нужно отдельно продумать очистку и проверить все уроки после загрузки.
- Первоначальная расшифровка урока 3 оказалась ненадёжной: чтение страницы 52 позже пришлось исправлять по исходной фотографии. Остальной ранее сгенерированный контент урока 3 при сомнениях также нужно сверять с фото.
- Основные коммиты: `a889c5d`, `2eefe4c`, `07ab328`.

### 2026-09-21 — Надёжность и скорость навигации

- Добавлены `error.tsx` и `global-error.tsx`, чтобы ошибка рендера или временная проблема сети показывала экран повтора вместо белого экрана PWA.
- Клиент Supabase, `auth.getUser()` и чтение профиля мемоизированы через `React.cache()` в пределах одного серверного рендера.
- Удалены повторные auth/profile-запросы из layout и страниц обучения; переходы между разделами стали заметно быстрее.
- Не убирать эти обёртки без замера: корневой layout и страницы обучения обращаются к одной и той же пользовательской сессии.
- Коммиты: `8ddddb2`, `d0348bd`.

### 2026-09-21 — Тренажёр грамматики и словарь 2급

- Для всех 79 грамматических конструкций второго уровня добавлены записи `grammar_exercises`, чтобы они появились в тренажёре.
- Проведён аудит слов уроков и добавлены отсутствующие словарные записи; отдельным последующим коммитом добавлено ещё 58 найденных слов.
- Тренажёр получает конструкции через связь с `grammar_exercises`; одной записи в `grammar.json` недостаточно для появления грамматики в тренировке.
- После изменений словаря или грамматики недостаточно обновить JSON: требуется соответствующий production-импорт и проверка через интерфейс или REST.
- Коммиты: `a889c5d`, `d8f702b`.

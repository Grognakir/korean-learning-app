-- font_latin была заведена впрок в исходной миграции профиля и до сих пор
-- нигде не читалась/не писалась. Переименовываем в font_ui — колонка
-- отвечает не только за латиницу, а за весь интерфейс на кириллице и
-- латинице разом (один шрифт покрывает оба алфавита). Колонка мертва,
-- переименование безопасно; RLS/GRANT на profiles уже покрывают все её
-- колонки, отдельно трогать не нужно.

alter table public.profiles rename column font_latin to font_ui;
